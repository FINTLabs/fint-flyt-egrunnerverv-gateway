fint:
  org-id: fintlabs.no
  flyt:
    base-url: http://localhost:8091
    resource-server:
      security:
        api:
          external:
            enabled: true
            authorized-client-ids: 2
  kafka:
    default-replicas: 1
spring:
  kafka:
    bootstrap-servers: localhost:9092
  webflux:
    base-path: vlfk-no
server:
  port: 8102